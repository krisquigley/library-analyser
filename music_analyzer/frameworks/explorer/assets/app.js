const controls=['tempo','harmony','energy','genre','mood'];
let state={current_track_id:null};
function text(el,value){el.textContent=value==null?'':String(value);return el;}
async function api(path, options){const r=await fetch(path, options); if(!r.ok) throw new Error(await r.text()); return r.json();}
function controlQuery(){return controls.map(n=>{const mode=document.querySelector(`[name=${n}]:checked`).value; return `control=${encodeURIComponent(`${n}:${mode}:1`)}`;}).join('&');}
async function setCurrent(id){state=await api('/api/current',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({track_id:id})}); await refresh();}
async function refresh(){state=await api('/api/state'); const list=await api('/api/tracks?limit=500'); renderTracks(list.tracks); if(state.current_track_id){renderDetail(await api('/api/tracks/'+encodeURIComponent(state.current_track_id))); renderCandidates(await api('/api/candidates?'+controlQuery()));} renderMap(await api('/api/projection'));}
function renderTracks(tracks){const ul=document.getElementById('tracks'); ul.replaceChildren(...tracks.map(t=>{const li=document.createElement('li'); li.className=t.handle===state.current_track_id?'current':''; const b=document.createElement('button'); text(b,t.display_label||t.handle); b.onclick=()=>setCurrent(t.handle); li.append(b); return li;}));}
function renderDetail(d){const root=document.getElementById('detail'); const parts=[document.createElement('h3'),document.createElement('p')]; text(parts[0],d.display_label||d.handle); text(parts[1],`Status: ${d.latest_run_status||'missing'} · Locations: ${d.available_locations}`); const fields=document.createElement('div'); for(const [name,f] of Object.entries(d.fields)){const div=document.createElement('div'); div.className='field'; text(div,`${name}: ${f.manual_text||JSON.stringify((f.automatic&&f.automatic.values)||f.summary_values)||f.effective_source}`); fields.append(div);} root.replaceChildren(...parts,fields);}
function renderCandidates(data){const ol=document.getElementById('candidates'); ol.replaceChildren(...data.candidates.map(c=>{const li=document.createElement('li'); text(li,`${c.display_label||c.track_id} — ${c.tier} ${c.score??''}`); return li;}));}
function renderMap(data){const canvas=document.getElementById('map'), ctx=canvas.getContext('2d'); ctx.clearRect(0,0,canvas.width,canvas.height); for(const p of data.tracks){const x=20+p.x*(canvas.width-40), y=20+(1-p.y)*(canvas.height-40); ctx.beginPath(); ctx.arc(x,y, p.track_id===state.current_track_id?6:4,0,Math.PI*2); ctx.fillStyle=p.track_id===state.current_track_id?'#d33':'#357'; ctx.fill();}}
function buildControls(){const root=document.getElementById('controls'); for(const n of controls){const div=document.createElement('div'); div.append(text(document.createElement('span'),n+': ')); for(const m of ['off','soft','hard']){const label=document.createElement('label'), input=document.createElement('input'); input.type='radio'; input.name=n; input.value=m; input.checked=m==='off'; input.onchange=refresh; label.append(input,document.createTextNode(m)); div.append(label);} root.append(div);}}
document.getElementById('undo').onclick=async()=>{state=await api('/api/undo',{method:'POST'}); refresh();}; document.getElementById('reset').onclick=async()=>{state=await api('/api/reset',{method:'POST'}); refresh();}; buildControls(); refresh().catch(e=>text(document.getElementById('detail'),e.message));
